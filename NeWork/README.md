В проекте рекомендуется использовать версии фреймворков и библиотек, актуальные на момент старта разработки. Например, Kotlin версии не ниже 1.9, 
Coroutines не ниже 1.7, Dagger Hilt не ниже 2.48, Retrofit не ниже 2.9.

Инструкция по работе над проектом
Работа с сервером
В проекте вам предстоит работать с сервером. API, которое предоставляет сервер, с примерами вызовов описано на странице http://94.228.125.136:8080/swagger-ui/index.html

Для доступа ко всем запросам вам понадобится ключ, который нужно прикладывать к каждому запросу в заголовок Api-Key. API_KEY = c1378193-bc0e-42c8-a502-b8d66d189617

Требования к интерфейсу
Вам необходимо разработать приложение, состоящее из следующих основных блоков пользовательского интерфейса:

1. Основной экран приложения.

Стартовый экран должен содержать:

нижнее меню с тремя кнопками: посты (ic_posts.xml), события (ic_events.xml) и пользователи (ic_people_outline.xml),
кнопку меню (ic_account.xml) в AppBar с возможностью перехода на вход в аккунт и регистрацию или просмотр профиля.
1.1. Экран со списком постов (item_posts.xml через activity_main.xml).

Этот фрагмент главного экрана содержит:

список постов,
кнопку + (ic_add.xml), нажатие которой ведёт на создание нового поста (fragment_new_post.xml) или диалог с предложением войти/зарегистрироваться.
Карточка поста (item_posts.xml) должна включать:

аватар автора поста;
имя автора;
дату публикации в формате dd.mm.yyyy HH:mm;
кнопку лайка (ic_like.xml или ic_un_like.xml) с количеством лайков;
вложение, при наличии: аудио, видео если есть (ic_play.xml) или фото;
ссылку, при наличии, с переходом в браузер;
кнопку вызова меню (ic_more_vert.xml) с возможностью удаления или перехода к редактированию, в случае, если текущий пользователь является автором;
текст поста.
При нажатии на карточку поста должен быть произведён переход к детальному виду (fragment_post_details.xml) с отображением дополнительной информации:

список упомянутых юзеров;
последнее место работы автора, либо текст «В поиске работы»;
карту с маркером (ic_location_on), при наличии координат.

детальный просмотр поста или события выглядит следующим образом вверху AppBar слева Название проекта справа иконка ic_share ниже шапка поста автарка\логин\ниде дата и время\справа у поста три точки   
ниже картинка поста ниже контент ниже слева иконка\кнопка лайк с цифрой количества лайков, правее круглые аватарки последних пяти человек лайкнувших данный пост если лайков больше пяти то шестым      
кругляшом вместо аватарки будет ic_add по клику на кружок откроется список юзеров лайкнувших данный пост. ниже иконка ic_people с анналогичной реализацией как у лайков только здесь список менторов    
упомянутых в посте. ниже мини блок карты (если есть) прикреплённой к этому посту. ниже по центру тонкая полоска примерно 8dp в толщину и 100dp в длину за неё можно вытащить нижний тулбар тач зона покрывает ручку шире и выше ручки на 6dp


1.2. Экран со списком событий (item_event.xml через activity_main.xml).

Этот фрагмент главного экрана содержит:

список событий,
кнопку + (ic_add.xml), нажатие которой ведёт на создание нового события (fragment_new_event.xml) или диалог с предложением войти/зарегистрироваться.
Карточка события (item_event.xml) должна включать:

аватар автора события;
имя автора;
дату публикации в формате dd.mm.yyyy HH:mm;
дату проведения в формате dd.mm.yyyy HH:mm;
тип события: офлайн или онлайн;
кнопку лайка (ic_like.xml или ic_un_like.xml) с количеством лайков;
вложение, при наличии: аудио, видео если есть (ic_play.xml) или фото;
ссылку, при наличии, с переходом в браузер;
кнопку вызова меню (ic_more_vert.xml) с возможностью удаления или перехода к редактированию, в случае, если текущий пользователь является автором;
текст поста.
При нажатии на карточку события должен быть произведён переход к детальному виду (fragment_event_details.xml) с отображением дополнительной информации:

последнее место работы автора, либо текст «В поиске работы»;
список участников;
список спикеров;
карту с маркером, при наличии координат.

1.3. Экран со списком пользователей (item_user.xml через activity_main.xml).

Этот фрагмент главного экрана содержит список пользователей.

Карточка пользователя должна включать:

логин пользователя;
имя пользователя;
аватар пользователя.
При нажатии на карточку пользователя должен быть произведён переход к детальному виду (fragment_user_details.xml.

2. Экран входа в приложение.

Необходимо использовать поля ввода для логина и пароля.

При этом должны проверяться основные ограничения на значения указанных полей:

логин — непустая строка;
пароль — непустая строка.
При несоответствии требованиям в полях должны отображаться определённые информативные сообщения с возможностью исправления данных.

В случае получения от сервера 400 кода следует отобразить ошибку в виде тоста «Неправильный логин или пароль».

3. Экран регистрации.

Необходимо использовать поля ввода для логина, имени, пароля и повтора пароля. А также кнопку с возможностью выбора фото и её превью.

При этом должны проверяться основные ограничения на значения указанных полей:

логин — непустая строка;
имя — непустая строка;
пароль — непустая строка;
аватар — изображение в формате jpeg, png, максимальный размер 2048*2048.
При несоответствии требованиям в полях должны отображаться определённые информативные сообщения с возможностью исправления данных.

В случае получения от сервера 400 кода следует отобразить ошибку в виде тоста «Пользователь с таким логином уже зарегистрирован».

4. Экран создания/редактирования поста.

На этот экран может попасть только авторизованный пользователь.

Экран содержит:

поле ввода текста;
кнопку выбора локации: переход на фрагмент с картой;
кнопку выбора упомянутых пользователей (экран со списком и множественным выбором);
кнопки выбора изображения: фото или галерея;
кнопки выбора вложения: аудио, видео;
кнопку выбора локации;
кнопку сохранить в AppBar.
Размер вложений не должен превышать 15 МБ.

После создания поста следует возврат назад к списку постов.

5. Экран создания/редактирования события.

На этот экран может попасть только авторизованный пользователь.

Экран содержит:

поле ввода текста;
кнопку выбора локации: переход на фрагмент с картой;
radio button с выбором типа между online и offline (по умолчанию online);
кнопку выбора даты проведения события;
кнопки выбора изображения: фото или галерея;
кнопки выбора вложения: аудио, видео;
кнопку выбора спикеров: диалог со списком пользователей;
кнопку сохранить в AppBar.
Размер вложений не должен превышать 15МБ.

После создания события следует возврат назад к списку событий.

6. Экран просмотра пользователя.

Экран содержит:

имя и логин в AppBar;
фото пользователя;
табы с выбором между стеной и работами пользователя.
6.1. Экран просмотра стены.

Стена представляет собой список постов, написанный одним автором. Внешний вид аналогичен карточкам из стартового экрана. Для получения стены используйте api.

6.2. Экран просмотра списка работ.

Карточка работы должна включать:

название компании;
стаж в формате dd MMM yyyy;
должность.
Для получения списка работ воспользуйтесь запросом.

7. Экран просмотра своего профиля.

Функционал и внешний вид этого экрана аналогичен просмотру пользователя, но включает возможность удалять и создавать работы.

Общие требования к интерфейсу приложения:

должна быть максимально реализована концепция Single Activity — весь переменный контент в приложении при помощи фрагментов или @Composable функций;
все страницы экрана должны содержать меню в AppBar, которое формируется в зависимости от состояния аутентификации пользователя: кнопки «Вход», «Выход» и «Регистрация»;


Приложение должно использовать сервер http://94.228.125.136:8080/swagger-ui/index.html#/.

В проекте не должно быть неспользуемых библиотек и ресурсов.

Все предупреждения от IDE должны быть либо исправлены, либо подавлены.

Приложение должно использовать сервер http://94.228.125.136:8080/.

Для запросов используйте ключ: c1378193-bc0e-42c8-a502-b8d66d189617
//Проанализируй весь код этого проекта. в файле README.md задание




[//]: # (детальный просмотр поста или события выглядит следующим образом вверху AppBar слева Название проекта справа иконка ic_share ниже шапка поста автарка\логин\ниде дата и время\справа у поста три точки   )

[//]: # ( ниже картинка поста ниже контент ниже слева иконка\кнопка лайк с цифрой количества лайков, правее круглые аватарки последних пяти человек лайкнувших данный пост если лайков больше пяти то шестым      )

[//]: # (   кругляшом вместо аватарки будет ic_add по клику на кружок откроется список юзеров лайкнувших данный пост. ниже иконка ic_people с анналогичной реализацией как у лайков только здесь список менторов    )

[//]: # (  упомянутых в посте. ниже мини блок карты &#40;если есть&#41; прикреплённой к этому посту. ниже по центру тонкая полоска примерно 2 мм в толщину и 2 см в длину за неё можно вытащить нижний тулбар )

[//]: # ()
[//]: # (проанализируй полностью проект и просчитай изменения по кнопке fab открывается создание поста или события и для этих окон нижнее меню должно менятся должны быть иконки ic_photo, ic_attach,            )

[//]: # (   ic_people, ic_location вызов соответствующих функций необходимо перенести с fragment_new_event и fragment_new_post соответственно кнопка save должна быть вынесена на верхнюю панель и иметь иконку     )

[//]: # (   ic_save  )

[//]: # ()
[//]: # (Проанализируй проект полностью нужно удалить промежуточный экран после нажатия кнопки + если открыта вкладка <item                                                                                     )

[//]: # (           android:id="@+id/postsFragment"                                                                                                                                                                 )

[//]: # (           android:icon="@drawable/ic_posts"                                                                                                                                                               )

[//]: # (           android:title="@string/posts" /> то при нажатии + автоматически открывается fragment_new_post. если открыта вкладка <item                                                                       )

[//]: # (           android:id="@+id/eventsFragment"                                                                                                                                                               )

[//]: # (           android:icon="@drawable/ic_events"                                                                                                                                                             )

[//]: # (          android:title="@string/events" /> то при нажатии + автоматически открывается fragment_new_event. если открыт профиль на вкладке wall то по нажатию + открывается fragment_new_post. если        )

[//]: # (  профиль открыт на вкладке Jobs то по нажатию + открывается fragment_new_job    )